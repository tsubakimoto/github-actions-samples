name: Hello world

on:
  push:

jobs:
  greet:
    name: Greet
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Echo
        run: echo "Hello world"
