name: GitHub Actions Context Dump

on:
  workflow_call:
    inputs:
      config-path:
        required: true
        type: string
    secrets:
      envPAT:
        required: true

  workflow_dispatch:

jobs:
  dump:
    runs-on: ubuntu-latest

    step:
      - uses: actions/checkout@v2
      - run: echo ${{ github }}
