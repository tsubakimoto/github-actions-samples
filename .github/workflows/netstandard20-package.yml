# https://github.com/dotnet/eshop
name: Publish a package for netstandard2.0

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          global-json-file: './dotnet/global.json'
      - name: Build
        run: |
          dotnet build --configuration Release
      - name: Pack
        run: |
          dotnet pack --no-build --configuration Release
      - name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: nuget-packages
          path: '**/*.nupkg'
      # - name: Publish
      #   run: |
      #     dotnet nuget add source "https://nuget.pkg.github.com/tsubakimoto/index.json"
      #     dotnet nuget push **/*.nupkg --source https://api.nuget.org/v3/index.json --api-key ${{ secrets.NUGET_API_KEY }}
